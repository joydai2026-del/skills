# Done Calibration: <feature name>

> An AI feature is "done" when all five layers below are green. Half-green is not done.
> Generated by /ai-done. Source: Gothelf, "What 'done' means when shipping AI features"
> (https://jeffgothelf.com/blog/what-done-means-when-youre-shipping-ai-features/).

- Feature: <name>
- Owner: <name>
- Date: <YYYY-MM-DD>
- Status: DRAFT | CALIBRATED

## Surface split

- Deterministic surfaces: <auth, billing, navigation, persistence, ...>
- Probabilistic surfaces: <model outputs, agent decisions, generated content, rankings, ...>

## L1: Deterministic floor  [ ] green

- qa-checklist: <path> (via /success-criteria, executed by the checklist loop)
- Global QA base (verified directly, not by /success-criteria): static analysis <pass>, tests <green>, device / real-env <run>

## L2: Distributional quality  [ ] green

- Eval suite: <path> (via /eval-harness)
- Quality bands (written as ranges):
  - Category X: at least <e.g. 80%> meet bar Y; the tail (<e.g. 20%> or fewer) degrades to <acceptable behavior>, never <embarrassing or harmful behavior>.
- Graders: <code / model / human>
- Passing baseline: <metric = value, date>

## L3: Failure-triage playbook  [ ] green

> Solo team: the owner cell holds a pre-decided ACTION + escalation trigger, not a name.

| Failure class | Owner / pre-decided action | First action | Escalate to |
|---|---|---|---|
| Model quality | | | |
| UX | | | |
| Content | | | |
| Safety / PR | | | |

## L4: Tripwires + rehearsed rollback  [ ] green

Tripwires:

| Metric | Threshold = failing | Alarm destination (real channel) |
|---|---|---|
| | | |

Rollback steps:

1. <flip feature flag off / revert model or prompt / fall back to deterministic path>

Rehearsal evidence (REQUIRED, must be a real artifact, not a prose claim; un-rehearsed = not done):
- Environment: <staging / controlled prod drill>
- Command run + actual output (or log line / screenshot path): <paste>
- Timestamp: <YYYY-MM-DD HH:MM>

## L5: Post-ship eval loop  [ ] green

- Capture (where prod outputs are logged): <request log / database table / storage volume / observability tool>
- Sample (how a sample is drawn): <N random per period + every tripwire-flagged>
- Feed back (failure becomes a new /eval-harness case): <how>
- Reviewer + cadence: <name, day, sample size>
- Outcome metric (customer actually succeeded, not just shipped; the house rule check applied post-ship): <metric>

## Readiness

| Layer | Status |
|---|---|
| L1 Deterministic floor | |
| L2 Distributional quality | |
| L3 Failure triage | |
| L4 Tripwires + rehearsed rollback (artifact, not prose) | |
| L5 Post-ship eval loop | |

DONE only if all five are green.
